import { port } from '@oxenode/core'
    
/**
* Node's Name
*/
export const Name = "{{NODE_NAME}}";

/**
* Node's React Component
*/
export default function Content({ }) {
    return <h1>{{ NODE_NAME }}</h1>;
}

/**
 * onFetch myValue Callback
 */
const onFetchMyValue = ({ state, input }) => {
    // Use state or input if needed

    return "Hello from oxenode"
}

/**
* Node's Ports
*/
export const ports = [
    port.output().type('string').label('myValue').onFetch(onFetchMyValue)
];